{
  "$schema": "https://opencode.ai/config.json",
  "provider": {},
  "agent": {},
  "permission": {
    // Deny search_rag globally; the rodi subagent overrides this to `allow`
    // in its frontmatter, so only rodi can query the Rodi RAG endpoint.
    "search_rag": "deny",
  },
  "mcp": {},
  "tools": {
    "github-triage": false,
    "github-pr-search": false,
  },
}
